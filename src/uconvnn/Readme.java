/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package uconvnn;

/**
 *
 * @author bowen
 */
public class Readme {
    /*
    Merge Foward, backward and update into single kernel, and compare performance with three separate kernels
    Add layer class, and non-linear classes
    Add interface for weight initialization, pass all parameters to allow special weight initialization
    Add interfaces for layers, so people can code custom layers
    Add automatic stride/padding calculations when given an input and output specifications
    Add fully connected layer and other convolutions, see torch layers
    Add interfaces for backprop algorithms so there can be custom backprop
    */
}
